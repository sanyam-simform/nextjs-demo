// Required default for the @modal parallel slot (Next.js 16 requires explicit
// default.tsx). Renders nothing when no interception is active.
export default function ModalDefault() {
  return null;
}
